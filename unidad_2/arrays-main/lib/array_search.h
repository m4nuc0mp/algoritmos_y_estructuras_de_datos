#ifndef _ARRAY_SEARCH
#define _ARRAY_SEARCH

int sequential_search(int key, int a[], int n);
int sequential_sorted_search(int key, int a[], int n);
int binary_sorted_search(int key, int a[], int n);

#endif
